--- SOURCE ---
func main(): s32 begin
    var x: s32, y: s32, z: s32
    x := 10
    y := 20
    z := 30
    x := y + z
    y := 5 + z * 3
    z := z * z - 3
    x := (x + y) / z
    x := y << 1
    y := x >> 1
    print x + y + z
end
--- OUTPUT ---
1182
